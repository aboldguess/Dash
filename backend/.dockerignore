# .dockerignore
# Purpose: Prevents unnecessary files from being included in the Docker build context.
# Structure: Entries grouped by category.

# Dependencies
node_modules

# Build artifacts
/dist

# Logs
*.log
